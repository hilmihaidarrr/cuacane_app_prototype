pragma Singleton
import QtQuick 2.0

QtObject {
    // 🔲 Warna Background
    property color darkBackground: "#121212"
    property color lightBackground: "#f0f0f0"

    // 🧱 Warna Card
    property color darkCard: "#1e1e1e"
    property color lightCard: "#ffffff"

    // 🎨 Warna Teks
    property color darkText: "#ffffff"
    property color lightText: "#000000"

    // ⬛ Border
    property color borderDark: "#2e2e2e"
    property color borderLight: "#cccccc"

    // 🌐 Warna Accent (opsional)
    property color accent: "#0078d7"
}
